<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Vouchers</title>
    <link rel="stylesheet" href="/styles.css" />
    <link rel="stylesheet" href="/styles/output.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/primeicons/6.0.1/primeicons.min.css">
</head>

<body class="bg-gray-100 font-sans">
    <%- include('partials/header') %>
    <%- include('partials/sidebar') %>

    <div id="base" class="p-6 ml-[113px] transition-all duration-700">
        <% if (vouchers.length === 0) { %>
            <div class="bg-gradient-to-r from-purple-500 to-indigo-600 text-white shadow-md rounded-lg mx-auto mt-6 w-[calc(100%-120px)] max-w-3xl">
                <div class="p-8 text-center">
                    <h1 class="text-4xl font-extrabold mb-4">No Vouchers Available</h1>
                    <p class="text-lg font-medium">You currently don’t have any vouchers. Check back later for exciting offers and rewards!</p>
                    <div class="mt-6">
                        <a href="/home" class="inline-block px-6 py-3 bg-white text-indigo-600 font-semibold rounded-md shadow hover:bg-gray-100 transition">
                            Return to Dashboard
                        </a>
                    </div>
                </div>
            </div>
        <% } else { %>
            <div class="flex flex-wrap justify-center mt-6">
                <% vouchers.forEach(voucher => { %>
                <div class="bg-white border border-gray-300 rounded-lg shadow-lg m-4 w-80 p-6 cursor-pointer hover:scale-105 transition duration-500 ease-in-out">
                    <div class="mb-4">
                        <h3 class="text-lg font-bold text-gray-800">Your Voucher</h3>
                        <p class="text-gray-900">You get $<%= voucher.value %>!!!</p>
                    </div>
                    <p class="text-gray-700">
                        <span class="font-semibold">Voucher ID:</span> <%= voucher.voucherID %>
                    </p>
                    <p class="text-gray-700">
                        <span class="font-semibold">Expiry Date:</span> <%= voucher.expiry_date.toISOString().split('T')[0] %>
                    </p>
                    <p class="text-gray-700">
                        <span class="font-semibold">Points:</span> <%= voucher.points %>
                    </p>
                    <p class="text-gray-700">
                        <span class="font-semibold">Mobile Number:</span> <%= voucher.mobileNo %>
                    </p>
                    <p class="text-gray-700">
                        <span class="font-semibold">Redeem Date:</span> <%= voucher.redeem_date ? voucher.redeem_date.toISOString().split('T')[0] : 'N/A' %>
                    </p>
                    <p class="text-gray-700">
                        <span class="font-semibold">Shop ID:</span> <%= voucher.shopid %>
                    </p>
                </div>
                <% }); %>
            </div>
        <% } %>
    </div>
    <script src="/scripts/sidebarHover.js" defer></script>
</body>

</html>